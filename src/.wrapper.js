(function(root, factory) {
    if (typeof define === 'function' && define.amd) {
        define(['jquery', 'jQuery.extendext'], factory);
    }
    else {
        factory(root.jQuery);
    }
}(this, function($) {
"use strict";

@@js

}));