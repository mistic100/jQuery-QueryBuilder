language: node_js
node_js:
  - "6"
before_install:
  - rvm install 2.3.3
  - gem install sass
  - gem install scss_lint
  - npm install -g grunt-cli
  - npm install -g bower
before_script:
  - bower install
after_success: grunt coveralls
